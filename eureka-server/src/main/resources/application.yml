spring:
  profiles:
    active: zipkin
  application:
    name: eureka-server

server:
  port: 8761

eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
  instance:
    instance-id: localhost
    prefer-ip-address: true